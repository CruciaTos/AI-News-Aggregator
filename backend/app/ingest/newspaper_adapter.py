"""Placeholder adapter for newspaper3k parsing.

Expose a minimal class with a `parse(url)` method to be implemented later.
"""


class NewspaperAdapter:
    """Adapter stub for newspaper3k."""

    def parse(self, url: str) -> dict:
        """Parse a web article at `url` and return normalized fields (stub)."""
        raise NotImplementedError("NewspaperAdapter.parse is a scaffold stub")
