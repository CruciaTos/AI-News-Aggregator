"""Logging setup helper (placeholder).

Call `configure_logging()` early during app startup.
"""
import logging


def configure_logging():
    """Configure basic logging for development (placeholder)."""
    logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(message)s")
